
# Need fuse for gocryptfs
extra_docker:
  - "--device=/dev/fuse"
  - "--cap-add=SYS_ADMIN"
  - "--security-opt=apparmor:unconfined"

extra_init: "gdrive"

mount_rw:
  - var_log: /var/log
  - /home/ken/share/gocryptfs/default: /root/gdrive
