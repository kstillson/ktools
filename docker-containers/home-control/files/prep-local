
ACCOUNTS_DONT_DELETE_REGEX="^(root|hc)"
GROUP_DONT_DELETE_REGEX="^(root|hc)"

# Fix ownersip.
chgrp -R hc /home/hc

# fix perms
chmod -R go+rX /etc/passwd /etc/group /etc/init /usr/lib/python3.9/site-packages
