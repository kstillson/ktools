
msgwait 0.5                                # ~disable init msg about "new screen" et al
screen -t post_init 39 sh -c 'sleep 1; screen -X msgwait 5' # then restore to 5 seconds

source ~/.screenrc

screen bash

select bash
